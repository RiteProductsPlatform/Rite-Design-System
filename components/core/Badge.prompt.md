A pill that states a record's status. Read-only — for a removable pill use `Tag`.

```jsx
<Badge tone="success" dot>Live</Badge>
<Badge tone="warning">Needs review</Badge>
<Badge tone="brand">ConvertRite</Badge>
```

Tones map to the semantic status tokens. Keep labels to one or two words, sentence case.
