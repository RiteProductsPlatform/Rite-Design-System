38×22 track that fills green when on. For settings that take effect immediately.

```jsx
<Switch checked={autoMap} onChange={setAutoMap} label="Auto-map matching columns" />
```

For a choice that only applies on save, use `Checkbox` instead.
