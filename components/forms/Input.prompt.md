Text field: 40px tall, 8px radius, 1px #E0E0E0, green focus ring.

```jsx
<Input placeholder="Search projects" icon="search" />
<Input defaultValue="240000" suffix="USD" />
<Input invalid placeholder="Required" />
```

`size="sm"` (32px) for filter bars and in-table editing. Focus is a 1px green border plus a 3px rgba(0,111,83,.25) ring.
