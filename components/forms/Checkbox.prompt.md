18px square, 4px radius, fills Tropical Rainforest when checked.

```jsx
<Checkbox checked={all} indeterminate={some} onChange={setAll} label="Select all rows" />
```

Use `indeterminate` on table header select-alls. Controlled only — pass `checked` and `onChange`.
