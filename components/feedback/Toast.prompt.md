Transient confirmation. Always Deep Forest — the toast is the one always-dark surface in a light screen.

```jsx
<Toast title="Mapping saved" onDismiss={hide}>42 columns matched automatically.</Toast>
```

Stack bottom-right with 8px gaps, auto-dismiss after ~5s. Use `Banner` for anything the user must act on.
