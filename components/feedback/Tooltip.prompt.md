Deep Forest label on hover or focus. One line, no title, no rich content.

```jsx
<Tooltip label="Adjusted utilisation, last 30 days"><Icon name="circle-help" size={14} /></Tooltip>
```

`IconButton` already sets a native `title`; only wrap it in `Tooltip` when you need the styled version.
