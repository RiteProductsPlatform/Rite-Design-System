Inset mint-tint track with a green fill. Used for load progress, coverage and completion.

```jsx
<ProgressBar value={68} label="Records loaded" showValue />
<ProgressBar value={12} tone="danger" size="sm" />
```

`size="sm"` (4px) inside table rows; `md` (8px) anywhere else.
