Centred nothing-here state. Icon plate by default; pass `illustration` for the brand's flat vector artwork.

```jsx
<EmptyState icon="database" title="No conversion runs yet"
  description="Start a run to see record counts, exceptions and load history here."
  action={<Button icon="play">Start a run</Button>} />
```

Copy follows the brand voice: state the fact, then say what happens next. No apology, no exclamation.
